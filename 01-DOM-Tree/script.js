// 1. Outputs the DOM tree as an interactive HTML/XML-like structure.
// Use this when you want to inspect the HTML hierarchy and nesting.
console.log(document);

// 2. Outputs a full list of the object's properties and methods.
// Use this when you need to see the "behind-the-scenes" JavaScript API 
// (e.g., checking .location, .referrer, or available event handlers).
console.dir(document);